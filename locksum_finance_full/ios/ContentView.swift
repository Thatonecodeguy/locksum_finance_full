import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Locksum Finance")
                .font(.largeTitle)
            Text("Connect to your FastAPI backend from here.")
        }
        .padding()
    }
}
