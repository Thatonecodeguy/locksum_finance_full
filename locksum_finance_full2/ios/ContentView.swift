import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Locksum Finance")
                .font(.largeTitle)
            Text("Connect this app to your FastAPI backend.")
        }
        .padding()
    }
}
